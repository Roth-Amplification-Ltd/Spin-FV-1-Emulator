import Foundation

extension Notification.Name {
    static let fv1OpenProgram =
        Notification.Name(
            "FV1OpenProgram"
        )

    static let fv1PasteSpinASM =
        Notification.Name(
            "FV1PasteSpinASM"
        )

    static let fv1OpenAudioLoop =
        Notification.Name(
            "FV1OpenAudioLoop"
        )

    static let fv1ShowAudioSettings =
        Notification.Name(
            "FV1ShowAudioSettings"
        )

    static let fv1RefreshAudioDevices =
        Notification.Name(
            "FV1RefreshAudioDevices"
        )

    static let fv1ShowGeneratorSettings =
        Notification.Name(
            "FV1ShowGeneratorSettings"
        )

    static let fv1ShowAudioLoopRegion =
        Notification.Name(
            "FV1ShowAudioLoopRegion"
        )

    static let fv1SetAnalyzerFFTSize =
        Notification.Name(
            "FV1SetAnalyzerFFTSize"
        )
}
